//  API Configuration
// change baseUrl to backend URL when done
// useMockData controls whether the app hits the real API or uses local data.

class AppConfig {
  static const bool useMockData = true; // change to false when API is ready

  //Replace with real backend URL
  static const String baseUrl = 'http://localhost:5000/api';
}
