## Setup
1. Install:
   npm install
2. Create env file:
   Copy `.env.example` to `.env`
   Paste `openssl rand -base64 32` in the terminal to generate a JWT secret
   Fill in the values
## Execution
  npm start
